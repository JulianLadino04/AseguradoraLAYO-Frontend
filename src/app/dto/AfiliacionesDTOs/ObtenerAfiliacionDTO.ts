export interface ObtenerAfiliacionDTO {
    id: string;  // Añadido para identificar la afiliación única
    cedula: string;
    nombre: string;
    correo: string;
    telefono: string;
    direccion: string;
  }
  